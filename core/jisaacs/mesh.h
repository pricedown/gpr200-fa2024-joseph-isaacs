struct Vertex {
	
};

struct MeshData {
	// vertices
	// indices
};

class Mesh {
	public Load() {
		// attribs
		// sizeof(Vertex), (const void*)offsetof(Vertex, pos)

		// sizeof(Vertex) * meshData.vertices.size();
	}
};